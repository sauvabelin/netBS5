symfonycasts_reset_password:
    # Will be replaced with NetBS\SecureBundle\Repository\ResetPasswordRequestRepository
    # once the entity + repository are created (plan Task 3).
    request_password_repository: symfonycasts.reset_password.fake_request_repository
