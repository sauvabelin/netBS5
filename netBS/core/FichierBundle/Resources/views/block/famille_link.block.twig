{% if app.user.hasRole('ROLE_SG') %}
    <a href="{{ path('netbs.fichier.famille.remove', { id: famille.id}) }}" class="btn btn-outline-danger w-100 mt-2" data-turbo-confirm="Etes-vous VRAIMENT sûr? Toutes les données liées (membres, factures etc.) seront supprimées et peuvent entraîner des erreurs de données." data-turbo-method="post">Supprimer cette famille</a>
{% endif %}
