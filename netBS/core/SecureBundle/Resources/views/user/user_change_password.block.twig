{{ form_start(cpForm) }}
{{ form_errors(cpForm) }}
{# Hidden identity field so password managers update the right account entry
   rather than staying silent on a page that only contains password fields. #}
<input type="text" name="username" value="{{ app.user.userIdentifier }}" autocomplete="username" readonly hidden>
{{ form_row(cpForm.old_password) }}
{{ form_row(cpForm.new_password) }}
<button class="btn btn-primary">Changer de mot de passe</button>
{{ form_end(cpForm) }}
